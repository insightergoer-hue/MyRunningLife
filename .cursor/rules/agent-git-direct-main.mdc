---
description: 禁止开 PR；所有修改直接推 main（本人强制）
alwaysApply: true
---

# Git 工作流：禁止开 PR（本人 2026-08-27 强制 · 同日再确认）

**本人要求：以后不再开 PR。所有修改直接合入 `main`。**

## 必须做

1. 在 **`main`** 上直接改
2. `git add` → `git commit` → **`git push origin main`**
3. 交付完成以 **main 上有 commit** 为准

## 禁止做

1. **禁止** `gh pr create` / `ManagePullRequest create_pr` / 新开任何 Pull Request
2. **禁止** 为「规范流程」再开 `cursor/...` 功能分支挂草稿 PR
3. **禁止** 改完只留在分支、等用户手动合 PR

## 例外（仅此一条）

仅当 `git push origin main` **被权限拒绝**时：在对话里说明阻塞，再临时用分支；**不要**默默改回开 PR 习惯。
