# Git 工作流（本人 2026-08-27 起强制）

**本人要求：不用开 PR；以后所有修改直接合入 `main`。**

## 执行

1. 在 `main` 上改（或短分支改完后立刻 `merge`/`ff` 进 `main`）
2. `git commit` → **`git push origin main`**
3. **禁止**为交付再开 Pull Request / `ManagePullRequest create_pr`
4. 已有草稿 PR 可忽略或自行关掉；新工作不要再挂 PR

## 例外

仅当 `push` 到 `main` 被权限拒绝时，才临时用分支，并在对话里说明阻塞原因（不要默默改回开 PR 流程）。
