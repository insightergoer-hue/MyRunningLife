name: Garmin daily sync

on:
  schedule:
    # 北京 09:00 — 每日一次（赶在 10:05 训练提醒前）
    - cron: "0 1 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "Sync mode"
        required: true
        default: morning
        type: choice
        options:
          - morning
          - yesterday
          - today
          - days
          - date
      days:
        description: "When mode=days: how many days through yesterday"
        required: false
        default: "3"
      date:
        description: "When mode=date: YYYY-MM-DD"
        required: false

jobs:
  sync:
    permissions:
      contents: write
    uses: ./.github/workflows/garmin-sync-reusable.yml
    with:
      mode: ${{ github.event.inputs.mode || 'morning' }}
      days: ${{ github.event.inputs.days || '3' }}
      date: ${{ github.event.inputs.date || '' }}
    secrets: inherit
